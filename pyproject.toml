[project]
name = "asteroids"
version = "0.1.0"
description = "A basic version of the classic asteroids game built for a boot.dev course"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]
